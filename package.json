{
  "name": "webdreamt",
  "version": "0.0.0",
	"description": "A list of grunt tasks needed to run the associated build tasks for WebDreamt.",
  "devDependencies": {
    "grunt": ">=0.4",
    "grunt-contrib-uglify": ">=0.5",
		"grunt-contrib-cssmin": ">=0.10",
		"grunt-contrib-concat": ">=0.5",
		"grunt-contrib-watch": ">=0.6"
  }
}